# CLAUDE.md — PACHI TRACKER 開発ガイド

## プロジェクト概要

パチンコ店内で稼働中に使うセッション記録・期待値計算アプリ。
ユーザーは片手操作・騒音環境・時間的プレッシャーの中で使う。

-----

## 設計原則

### 1. UIは「店内で片手で使える」が最優先

ユーザーはパチンコ台の前で、玉を打ちながら操作する。
あらゆるUI変更は「操作ステップが1つでも減るか？」で判断する。

- タップ領域は最低44px以上、理想は48px以上
- 視認性 > 美しさ。ダークテーマ・高コントラスト・大きな数字
- 装飾的な要素は追加しない。情報密度を上げるための工夫は歓迎する
- 迷ったら「忙しい人が3秒で理解できるか？」を基準にする
- 実戦中の入力速度を優先する
- タップ数増加を伴う改修は禁止
- 入力フロー変更時は理由を説明する
- iPhone 16 Pro相当の画面幅を優先してUI確認する
- 横スクロール前提UIは禁止

### 2. 計算ロジック（logic.js）は心臓部

`logic.js` にある期待値計算・仕事量・ボーダー計算は、このアプリの存在意義そのもの。
数式の誤りは「ユーザーの金銭的損失」に直結する。

**守るべきルール：**

- `deriveFromRows`, `calcCash`, `calcMochi`, `useLS` は既に検証済みの関数。変更する場合は、変更前後の計算結果を境界値（0, 極端に大きい値, 負数）で比較して提示すること
- `rotRows` が回転数の唯一の真実源（Single Source of Truth）。ここを迂回するデータフローを作らない
- 現金（calcCash）と持ち玉（calcMochi）の計算パスは明確に分離されている。混ぜない
- 期待値計算の基本式を変更する場合は、数学的根拠を説明してから実装する
- src/logic.js は変更禁止。変更が必要に見える場合は実装せず報告のみ行う
- 既存の定数・初期値・マスタデータ・デフォルト値を勝手に変更しない
- 「改善目的」の値調整は禁止
- P-EVIDENCE関連ロジック・解析処理は、明示指示がない限り追加・変更しない

### 3. 状態管理との整合性

`App.jsx` の `useState` で状態を一元管理している。

- 新しい状態を追加する場合は、既存の状態との依存関係を確認してから実装する
- 矛盾が生じそうな場合は、実装を止めて設計案を提示する
- localStorageとの永続化ロジック（`useLS`）を壊さないこと
- 新しい状態管理方式を勝手に追加しない
- 既存state構造を優先する
- Firebase / Firestore / Supabase などの新規バックエンドは、既に導入済みでない限り追加しない
- 既存にバックエンドがない場合は、まず localStorage または既存の保存層で実装する

### 4. コードベースの安定性

52回のマージを経て現在の安定性がある。

- リファクタリングは「動作の軽量化」または「バグ修正」に直結する場合のみ行う
- 「きれいにしたい」だけの理由でファイル構成を変えない
- 既存のコンポーネント分割には理由があるので、統合・分割する前に既存構造の意図を確認する
- 単一ファイルへの過剰実装を避ける
- 既存構造に合わせて責務分離する
- 新規ライブラリ追加は必要最小限にする
- 既存ライブラリで実装可能なら追加しない
- package.json変更時は理由を明記する
- ついでの別機能改修は禁止

-----

## UI開発フェーズの分離

UI開発は、モチベーション維持と完成イメージ共有のための「見た目優先プロトタイプ」と、既存設計へ安全に統合する「安全な本実装」を明確に分ける。
見た目を先に作る場合でも、`logic.js`・判断ロジック・保存データ構造を壊してはいけない。

### 1. 見た目優先プロトタイプ

目的：

- 完成イメージを掴む
- モチベーションを上げる
- 画面の方向性を早く確認する
- 画像モックの世界観を実装UIに翻訳する

許可：

- UIコンポーネントの見た目変更
- CSS / className / レイアウト調整
- 大きなカード、判定バッジ、下部固定CTAなどの視覚改善
- 仮表示やプレースホルダーの追加

禁止：

- `logic.js` の変更
- 計算式の変更
- 保存データ構造の変更
- `rotRows` を迂回する新しいデータフロー作成
- `evDecision.js` の判断条件変更
- 既存値の意味変更
- ついでの別機能改修
- 既存UI・入力項目・表示項目を勝手に削除しない
- 不要に見える場合も、削除ではなく提案として報告する

ルール：

- 仮表示にはコメントで「将来連携予定」など意図を明記する
- 見た目を優先しても片手操作性を落とさない
- UIラベルは日本語のみ
- タップ領域は最低44px以上
- 店内で3秒で理解できる情報階層にする
- ダミーデータ・仮データ・モックデータを本番画面に残さない
- テスト用コードを残す場合は明示する

### 2. 安全な本実装

目的：

- プロトタイプを既存設計に統合する
- 状態管理と整合させる
- 計算ロジックとUI表示の意味を一致させる
- 保守できるコンポーネント構造にする

必須：

- `docs/HANDOVER.md` の確認
- `CLAUDE.md` の確認
- 変更前後の差分確認
- `npm run lint`
- `npm run build`
- 必要に応じて保護テスト実行

### CodexとClaude Codeの役割分担

Codex：

- 画像モックに近い見た目のプロトタイプ作成
- UIの視覚再現
- カード、余白、色、レイアウトの試作
- ただし計算ロジックには触らない

Claude Code：

- 既存コードとの整合性確認
- 状態管理の安全確認
- `logic.js` 保護
- 設計整理
- 本実装への安全な統合

### 見た目優先プロトタイプの報告ルール

見た目優先プロトタイプを行った場合も、完了報告には以下を含める：

1. 変更したファイル
1. 見た目の変更内容
1. 操作ステップへの影響
1. `logic.js` を変更していないこと
1. 計算式を変更していないこと
1. `npm run lint` の結果
1. `npm run build` の結果

-----

## 実行ルール

### ビルド検証（必須）

ファイルを作成・修正したら、必ず以下を実行してからコミットする：

```bash
npm run lint
npm run build
```

両方がエラーゼロで通ること。警告は内容を確認して報告する。

### コミットメッセージ

日本語で、変更内容が一目でわかるように書く。

```
fix: サポ増減の計算で電サポ回転数が0の場合のゼロ除算を修正
feat: 大当たり記録画面にサポ開始/終了時持ち玉入力フィールドを追加
refactor: rotRows導出ロジックの不要な中間変数を削除
```

### 変更の報告

実装完了時に以下を含めること：

1. 何を変えたか（差分の要約）
1. なぜ変えたか（目的）
1. 計算ロジックに触れた場合 → テスト結果
1. UIに触れた場合 → 操作ステップ数の変化
1. 追加・変更したデータ構造
1. package.json変更有無
1. 未対応・保留事項と理由
1. `src/logic.js` を変更していないこと
1. 不要なconsole.logやdebugコードを残していないこと

-----

## 技術スタック

- React（JSX）
- ダークテーマベース
- localStorage永続化
- 日本語のみ（UIラベルに英語を使わない）
